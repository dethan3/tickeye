# TickEye 基金监控项目依赖

# Python 版本要求: >= 3.8 (推荐 3.9+)

# 核心数据获取
akshare>=1.12.0          # 基金数据获取 (金融数据接口)
requests>=2.28.0         # HTTP客户端 (AKShare 依赖，飞书通知)

# 数据处理和分析
pandas>=1.5.0            # 数据处理和分析 (核心依赖)