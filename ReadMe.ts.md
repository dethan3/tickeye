# TickEye (TypeScript version)

## Usage

```shell
git clone https://github.com/dethan3/tickeye
cd tickeye

pip install  # for Python packages
pnpm install  # for Node.js packages

npm test  # run Node.js script locally for debugging
```
